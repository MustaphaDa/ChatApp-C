<Window x:Class="LoginPage.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="200" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Username:" Grid.Row="0" Margin="10"/>
        <TextBox x:Name="UsernameTextBox" Grid.Row="0" Margin="80,10,10,10"/>

        <Label Content="Password:" Grid.Row="1" Margin="10"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Margin="80,10,10,10"/>

        <Button Content="Login" Grid.Row="2" Margin="10" Click="LoginButton_Click"/>
    </Grid>
</Window>
